[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-usage-status"
version = "0.1.0"
description = "Token-free Codex quota monitor using the local app-server protocol"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
